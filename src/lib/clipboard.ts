import { GraphicsItem } from "./scene/graphicsitem";
import { NodeScene } from "./scene";

export class ItemClipboard {
	public static copyItems(scene: NodeScene, clipboard: DataTransfer) {}

	public static pasteItems(scene: NodeScene, clipboard: DataTransfer) {}
}
